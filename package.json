{
  "name": "dental-management-dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start:dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "db:migrate": "npx prisma migrate dev --prefix server",
    "CLICK_THIS_TO_RUN_PROJECT": "npm run dev",
    "start": "npm run dev",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
